<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<property name="LOGS_ABSOLUTE_PATH" value="C:/logs/record" />

	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<layout class="ch.qos.logback.classic.PatternLayout">
			<!--<pattern>%d{yyyy-MM-dd HH:mm:ss} [%-5p] [%F]%C  %M\(%L\) : %m%n</pattern>-->
			<pattern>%d{yyyy-MM-dd HH:mm:ss} [%-5p] [%F]%C %M\(%L\) : %m%n</pattern>
			<!--<pattern>%highlight([%-5level]) [%d{yy-MM-dd HH:mm:ss}] %cyan(%logger{36})\n - %msg%n</pattern>-->
		</layout>
	</appender>

	<appender name="auth.out"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOGS_ABSOLUTE_PATH}/auth.out</file>
		<rollingPolicy
			class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${LOGS_ABSOLUTE_PATH}/auth.out.%d{yyyyMMdd}</fileNamePattern>
			<maxHistory>60</maxHistory> <!-- 일자별 백업파일의 보관기간 -->
		</rollingPolicy>
		<encoder>
			<pattern>%d{yyyy-MM-dd HH:mm:ss} [%-5p] [%F]%M\(%L\) : %m%n</pattern>
		</encoder>
	</appender>
	
	<appender name="db.out"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOGS_ABSOLUTE_PATH}/db.out</file>
		<rollingPolicy
			class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${LOGS_ABSOLUTE_PATH}/db.out.%d{yyyyMMdd}</fileNamePattern>
			<maxHistory>60</maxHistory> <!-- 일자별 백업파일의 보관기간 -->
		</rollingPolicy>
		<encoder>
			<pattern>%d{yyyy-MM-dd HH:mm:ss} [%-5p] [%F]%M\(%L\) : %m%n</pattern>
		</encoder>
	</appender>
	<!-- log4jdbc 옵션 설정 -->
	<!--  
		jdbc 			: 커넥션 open close 이벤트를 로그로 남긴다.
		jdbc.sqlonly    : SQL문만을 로그로 남기며, PreparedStatement일 경우 관련된 argument 값으로 대체된 SQL문이 보여진다.
		jdbc.sqltiming  : SQL문과 해당 SQL을 실행시키는데 수행된 시간 정보(milliseconds)를 포함한다.
		jdbc.audit      : ResultSet을 제외한 모든 JDBC 호출 정보를 로그로 남긴다. 많은 양의 로그가 생성되므로 특별히 JDBC 문제를
						  추적해야 할 필요가 있는 경우를 제외하고는 사용을 권장하지 않는다.
		jdbc.resultset  : ResultSet을 포함한 모든 JDBC 호출 정보를 로그로 남기므로 매우 방대한 양의 로그가 생성된다.
		jdbc.connection : SQL 결과 조회된 데이터의 table을 로그로 남긴다.
	-->
	<!--<logger name="jdbc" level="OFF" additivity="false">
		<appender-ref ref="STDOUT" />
	</logger> 
	
	<logger name="jdbc.connection" additivity="false">
		<level value="ERROR" />
		<appender-ref ref="auth.out"/> 
	</logger>-->
	<!--
	 log4jdbc 옵션 설정 
	<logger name="jdbc" level="OFF"/>
	 커넥션 open close 이벤트를 로그로 남긴다. 
	<logger name="jdbc.connection" level="OFF" additivity="false"/>
	 SQL문만을 로그로 남기며, PreparedStatement일 경우 관련된 argument 값으로 대체된 SQL문이 보여진다. 
	<logger name="jdbc.sqlonly" level="OFF" />
	 SQL문과 해당 SQL을 실행시키는데 수행된 시간 정보(milliseconds)를 포함한다. 
	<logger name="jdbc.sqltiming" level="INFO" additivity="false">
		<appender-ref ref="STDOUT"/>
	</logger>
	 ResultSet을 제외한 모든 JDBC 호출 정보를 로그로 남긴다. 많은 양의 로그가 생성되므로 특별히 JDBC 문제를 추적해야 할 필요가 있는 경우를 제외하고는 사용을 권장하지 않는다. 
	<logger name="jdbc.audit" level="OFF"/>
	 ResultSet을 포함한 모든 JDBC 호출 정보를 로그로 남기므로 매우 방대한 양의 로그가 생성된다. 
	<logger name="jdbc.resultset" level="OFF" additivity="false"/>
	 SQL 결과 조회된 데이터의 table을 로그로 남긴다. 
	<logger name="jdbc.resultsettable" level="OFF"/>
	
	<logger name="com.zaxxer.hikari.pool" level="OFF"/>
	
	<logger name="com.example.demo.web" level="INFO" additivity="false">
		<appender-ref ref="auth.out"/>
	</logger>
	-->
	<logger name="org.springframework.test" level="ERROR" />
    <logger name="org.springframework.web.servlet.mvc.method.annotation.RequestResponseBodyMethodProcessor" level="ERROR" />
    <logger name="jdbc.splonly" level="DEBUG" />
	<logger name="jdbc.sqltiming" level="INFO" />
	<logger name="jdbc.audit" level="WARN" />
	<logger name="jdbc.resultset" level="ERROR" />
	<logger name="jdbc.resultsettable" level="INFO" />
	<logger name="jdbc.connection" level="OFF" />
	<root level="INFO">
	  <appender-ref ref="STDOUT" />
	</root>
 
</configuration>